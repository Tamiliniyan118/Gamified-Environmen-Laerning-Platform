# GaiaQuest (dev quickstart)

Lightweight dev scaffold for a gamified environmental learning platform with photo upload + XAI explain flow.

## Install & run

1. Open a terminal and start the backend:
```bash
cd backend
npm install
npm run dev
